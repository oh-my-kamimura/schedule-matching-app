type CalendarMap = { [key: string]: [label: string, color: string]; };
const calendarMap: CalendarMap = {
	outdoor: ['外出', "#FF9900"],
	home: ['自宅', "#8FBC8B"],
	work: ['仕事', "#FFE944"],
};

export default calendarMap;